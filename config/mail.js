module.exports = {
  clienthall: {
    credentials: {
      accessKeyId: process.env.AWS_ACCESS_KEY_ID,
      secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY
    },
    email: "support@clienthall.com",
    subject: "Your feedback makes us better!"
  }
}